const GITHUB_USERNAME = "vcrahulraj";

const PROJECT_REPOSITORIES = Object.freeze({
  accessLens: "accesslens-iam-copilot",
  otTriage: "ot-security-triage-agent",
});

window.PORTFOLIO_CONFIG = Object.freeze({
  GITHUB_USERNAME,
  PROJECT_REPOSITORIES,
});
